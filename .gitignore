.DS_Store
build/
dist/
public/

# Swift Package Manager
.swiftpm/
.build/
Package.resolved

# Xcode per-user state (UI layout, breakpoints, personal schemes)
**/xcuserdata/
*.xcuserstate

# Local experiment / run logs (optional local only)
results.tsv
run.log

# Sparkle signing (never commit)
scripts/sparkle_eddsa_private.key

# Sparkle release staging (zip/delta); appcast.xml in docs/ is committed
docs/sparkle-archives/
