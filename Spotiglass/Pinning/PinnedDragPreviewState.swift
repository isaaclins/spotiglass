// Intentionally empty; the drag-preview singleton existed only to feed the
// pinned-sidebar drop-slot skeleton, which was removed together with the
// custom Library drag/drop machinery (native `List.onMove` needs no preview
// plumbing). `PinnedItemDragPill` remains as the plain `.onDrag` preview for
// drag-to-pin sources.
