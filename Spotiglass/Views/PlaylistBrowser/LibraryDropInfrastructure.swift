// Intentionally empty; the custom Library drag/drop machinery
// (LibraryPinnedItemDropDelegate, LibraryRowFramePreferenceKey,
// LibrarySidebarRowTransfer, drop slots, insertion-index geometry) was
// removed in favor of native `List.onMove` reordering plus a single
// `dropDestination(for: PinnedItemTransfer.self)` on the sidebar list
// (see `PlaylistBrowserSidebar`).
